<h1>Articles disponibles</h1>
<%= link_to "Nouvel article", new_article_path %>
<% @articles.each do |article| %>
  <h3><%= link_to "#{article.title}", article_path(article) %></h3>
  <p><%= article.content %></p>
  <h6>Last update : <%= article.created_at %></h6>
  <br>
<% end %>
